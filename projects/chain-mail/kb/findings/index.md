# Findings

Verified results and hard-won learnings, per milestone. Every claim here was measured with the
`scad-design-to-print` tooling (`check_fit.py`, `check_mesh.py`, `verify.py`) plus the project's
`tools/linking_number.py`.

* [M1 — print-in-place linkage](m1-print-in-place.md) — floating-islands, the linking-number bug, chosen gap
* [M2 — weave tiling](weave-tiling.md) — linking rules and the flat-tiling collision constraint
