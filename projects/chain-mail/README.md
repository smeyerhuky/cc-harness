# Chain Mail

A new playground project. Scaffolded with the standard structure; implementation to follow.

## Overview

This project was created from the playground project template. It provides the standard
skeleton — source, knowledge base, configuration, and metadata — ready for development.

- Source code directory (`src/`)
- Project-specific knowledge base (`kb/`)
- Project configuration (`CLAUDE.md`)
- Version information (`version.json`)

## Getting Started

Add your project code to the `src/` directory and documentation to the `kb/` directory.

## Status

`scaffold` — structure is in place, no implementation yet. Update this README and
`version.json` as the project takes shape.
